name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check .

      - name: Format check with black
        run: black --check .

      - name: Type check with mypy
        run: mypy apps/ shared/

      - name: Security scan (Safety)
        run: safety check --json

      - name: Dependency audit (pip-audit)
        run: |
          pip install pip-audit
          pip-audit --strict

      - name: Run tests
        run: pytest tests/unit/ tests/golden/ -v --tb=short --cov=apps --cov=shared --cov-report=term
        env:
          ENVIRONMENT: test

  frontend-tests:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: apps/web

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint (ESLint via next lint)
        run: npm run lint

      - name: Run tests
        run: npm run test

      - name: TypeScript check
        run: npm run type-check

      - name: Dependency audit
        run: npm audit --audit-level=high

  hosted-health-check:
    needs: [build]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Wait for Render deploy to propagate
        run: sleep 30

      - name: Check hosted API health
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://virtual-analyst-api.onrender.com/api/v1/health || echo "000")
          echo "Health check status: $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "WARNING: Hosted API health check returned $STATUS — may be cold-starting (Render free tier)"
          fi
        continue-on-error: true

  integration-tests:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: finmodel_test
          POSTGRES_PASSWORD: postgres
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U postgres -d finmodel_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/finmodel_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -e ".[dev]"

      - name: Install psql
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq postgresql-client

      - name: Apply migrations
        run: ./scripts/apply-migrations.sh apps/api/app/db/migrations

      - name: Run integration tests
        run: pytest tests/integration/ -v --tb=short
        env:
          INTEGRATION_TESTS: '1'
          ENVIRONMENT: test

  build:
    needs: [test, frontend-tests]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t virtual-analyst/api:${{ github.sha }} .

      - name: Scan image for vulnerabilities
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: virtual-analyst/api:${{ github.sha }}
          severity: HIGH,CRITICAL
          exit-code: "1"
          format: table
